import {createTheme} from '@mui/material/styles';

const theme = createTheme({
    palette: {
        primary: {
            main: 'rgb(190,94,164)',
        },
        secondary: {
            main: 'rgb(232,213,224)',
        },

    },
});

export default theme;
