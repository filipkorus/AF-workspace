import {createTheme} from '@mui/material/styles';

const theme = createTheme({
	palette: {
		primary: {
			main: 'rgba(173,11,93,0.44)',
		},
		secondary: {
			main: 'rgba(255,255,255,0.82)',
		},
	},
});

export default theme;
